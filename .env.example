# AeroPDF.dev environment (Specs §9)
PORT=8080
HOST=0.0.0.0
AEROPDF_API_KEY=local-dev-key
STORAGE_DRIVER=local
STORAGE_PATH=./aeropdf-data
MAX_UPLOAD_MB=50
RENDER_TIMEOUT_MS=60000
# ENABLE_HTML_PDF=off   # force structured renderer (skip Puppeteer)
